---
name: cake-design
revision: "3"
description: 宏观的 UX 交互设计指导文档，由人类编写，协助构建和谐、干练、洁净、实用、克制、灵动的交互体验。
disable-model-invocation: true
license: CC0-1.0
author: a1knla (l2a1knla@gmail.com)
---
# Overview

CakeDesign (CD) 是一份由人类编写、面向 AI 使用的 UX 指导文档，协助开发者构建理想中**和谐、干练、洁净、实用、克制、灵动**的交互体验。与常规设计指导文档不同，相比对每一处细节做出精细规定，CD 更偏向列举并否定反模式（Prefer "Don'ts" rather than "Dos"），相信这样的文档形式更有助于 AI 设计开发出符合 CD 的 UX 理想的优秀作品。

## 需特别注意

**CakeDesign 是自由的 UX 指导文档，平等地为所有用户带来优质的、风格鲜明的 UX 体验。其内容只专注于更佳的产出质量，不服务于任何具体企业、品牌、个人或商业转化目标，但明确服务于用户利益、诚实体验、可访问性、可验证性和软件自由。CakeDesign 尤其适合开源软件、自由软件、公共利益软件、个人项目、教育项目和非营利性软件使用，其设计理念以用户自身的真实使用体验为出发点，优先尊重用户的注意力、判断权、选择权、隐私、时间和退出能力，而并不以提升转化率、延长停留时间、制造依赖、放大商业曝光或引导消费为设计目标。**

# UI 规范

## 配色篇
### 通用部分

- 需要区分主色、副色、前景色、背景色，但无默认 Palette；
	- Palette 需要从现有项目中提取，或是向用户询问大致方向；
- 不过分强调 hover 和 active 状态；
- 万分审慎使用大块鲜明色彩。
### 阴影效果部分

**只允许**以下阴影使用方式：

- **通过阴影属性模拟 solid 的内边框；**
- **通过巨大的扩散半径与阴影尺寸制造极轻微的氛围感。**

除此之外，不可以其它方式使用阴影。
### 渐变色部分

- 需考虑不同显示器对色阶的分辨力差异。使用大尺寸渐变时，需知晓此效果在终端用户视角很可能并不平滑；
- 同一形状中，参与渐变的颜色不得超过 2 种，但相同颜色的出现次数不限制；
- 参与渐变的颜色，在 HSV 模型中，两两之间的色调（Hue）相差最好不超过 20.
### 蓝色疲劳

- 除非用户明确要求或现有色彩体系已经成型，否则避免主动使用接近下列范围的颜色作为主色：
  - #00a2e8 附近
  - #001bff 附近
  - #5fb0ff 附近
  - #3168ff 附近

## 动效篇

- 鼓励交互、鼓励通过交互引起局部小幅度动画播放；
- 不引入输入延迟，动效总是应当跟手；

### 动画曲线

  - C1: ease-in
  - C2: cubic-bezier(0.29, 0, 0, 1)
  - C3: linear

### 动画时长

  - T1: 150ms
  - T2: 200ms
  - T3: 250ms

### 可供参考的预设组合

  - 全局组件动效: C1 T3
  - 全局运动动效: C2 T2
  - 短促: C2 T1
  - 优雅: C2 T3
  - 细微: C3 T1

## 形状篇

### 圆角设计

- 禁止滥用超大圆角设计；
- 鼓励通过无边框融合代替直角边；
- 禁止将 Grid 布局与带有大比例圆角边框的 Cell 搭配在一起，可通过 hover 显示避免；

### 字形规范

- 避免使用极细（light / extra-light）字形。

#### 字体装饰

- 根据现有项目规范或询问用户，确定是否使用有衬线中文字体；
  - 使用有衬线字体时，需建议用户适当放大整体缩放尺寸
- 禁用 ligature；
- 禁用 italic, underlined 和 strikethrough style.

#### 字体选用

- 避免使用 `微软雅黑`，`微软雅黑 Light` 等矮字形字体；
- 禁用 Wingdings 类图标字体；
- （仅 Web 开发）鼓励覆盖 h1, h2, h3 和 h4 标签样式，避免浏览器默认行为；
- 相邻文本禁止出现字号跨层，相邻字号的像素高度相差不得超过 30%.

#### 字符选用

- 避免使用 Emoji 符号。

#### 关于商用字体

CD 不考虑商用限制因素，用户需自行决策。

## 排版篇

### 内容分布

- 鼓励使用无边框融合代替明确的模块间边界；
- 避免出现超过 3 个模块将页面空间充分分割、且模块间隔有明显的边框的情形。

### 避免无效信息展示

#### 避免在醒目区域展示软件本身的图标和名称

这是一个典型常犯错误：用户并不需要人提醒软件的名字是什么，特别是涉及品牌名称的部分。如果离开品牌商视角、站在用户视角思考，将会更加容易理解这个做法。

#### 避免无条件展示功能分区的名称

应优先追求使功能区域一目了然，其次才考虑为用户引入阅读成本。举例来说，一款常规计算器软件绝不会专门写上 “数字输入区”、“结果展示区” 的文案——它的冗余程度太高了。

#### 避免过早披露业务细节

一个典型例子：名为 “上传文件（最大64MB）” 的按钮。相比之下，在旁边引入低透明度的问号图标配上 hover 显示文案是更加偏好的设计。

进一步来说，很多按钮天然带有实心背景色，**当你在增加按钮文案的同时，也在扩张实心色块的面积。** 如果最终制造出一个醒目的大色块，只为时刻提醒用户 “文件不能超过 64 MB”，将是得不偿失的。

## 文案篇

- 慎用括号、引号；
- 谨慎引入达到 3 行或以上的纯提示文本；
- 反复斟酌，排除二义性、潜在的形成人格冒犯的可能；
- 慎用口语化的、刻意亲切化的文案；
- 避免直接描述 CD 指导内容的文案；
- 避免使用专用于表达 “看，我做了这个” 的文案；
- 注意甄别旧式文字：
	- 例如根据中国大陆的《部分计量单位名称统一用字表》，“屏幕尺寸为 27 吋” 应改为 “屏幕尺寸为 27 英寸”。
- 避免使用以下词汇：
	- 点击切换/点击前往中的 “点击” 动词（冗余度高）；
	- 为你、为您（给用户 “被特殊对待” 的信号，可能产生负面效果）；
	- 高度符合以上模式的其它词汇。

# 行为规范

## 广告行为篇

CD 对广告（Ads）的定义如下：

**广告不是一种样式，而是一种意图。只要一项信息的展示、排序、推荐或包装，是为了使用户更可能注意、信任、选择、购买、使用、访问、注册、订阅、支持或传播某个对象，并且该结果会直接或间接使发布者、委托者、平台、品牌方、销售方或相关利益方获益，该信息即构成广告。** 但由国家机关官方提供的、为了满足法律义务、公共安全、紧急避险、用户生命健康、财产安全、基本权利保障或服务正常运行所必需，且以中性、必要、最小化方式呈现的信息，不应仅因其影响用户行为而被视为广告。

CD 对于广告 UX 的规则是：

- 无条件禁止使用广告（Absolutely No Ads），且
- 须清晰调研并拒绝使用或加载含有广告投放能力的第三方 SDK。

### 关于 “可接受的广告（Acceptable Ads）”

- 如果 eyeo 团队在定义 “什么是可接受的广告” 时没有征求过用户本人的意见，那么它就无权代表用户接受特定的广告；
- 确切地说，在 CD 理想内的 UX 中：**没有什么广告是可以接受的。**

### 避免非必要的 Consent

在拒绝广告行为的基础上，CD 进一步激进地将一部分非营利性的干扰行为视为应当避免的行为。

- 欧盟法律所提出的 Cookie 同意弹窗
- 不可绕过的用户协议、隐私协议等弹窗
- 权限申请的前置弹窗（Soft Ask）

尽管相当一部分行为是必要的，甚至是部分地区的法律强制要求的，但是正如前文所述，CD 不以盈利为目标，是极端用户利好的设计指导文档——请最大限度减少这些干扰内容：

- 熟读欧盟对此的具体条款，选择性放弃基于 Cookie 做个性化推荐等非必要功能，只使用技术上严格必要的 Cookie，例如身份鉴权，从而免于弹出 Consent；
- 或是熟读地区法律例如中国大陆的《App违法违规收集使用个人信息行为认定方法》，选择性放弃任何针对用户个人信息的处理，从而免于弹出 Consent；
- 放弃服务于相关国家或地区。特别是对于非营利性软件而言，开发者更加不应当无限的承担合规成本，不合规便不服务是常见做法。

## 异常处理篇

### 事出有因

几乎不存在真正的“未知错误”。由用户主动发起的操作遇到故障时，必须清晰明确给出错误原因。一些典型错误示范：

- “暂时无法执行此操作，请稍后再试。”
- “数据异常，请稍后再试。”
- 静默失败

一些值得学习的正确示范如下。

- `开启失败，因为你的账号没有足够的权限（3001）。[刷新缓存并重试] [取消]` —— 错误源于软件自身，换句话说：**是你故意将用户阻断的。** 此时，必须如示例一样：
	- 提供立即从故障中恢复的尝试入口；
	- 诚实披露真实具体的错误原因；
	- 诚实披露**极大限度的**技术细节。若碍于篇幅，则提供可查询的错误码；
	- 保护用户可以随时退出的权利。

- `更新失败，因为 C 盘硬盘空间不足。 [查看占用情况] [取消]` （当用户点击查看占用情况，唤起 explorer 并切到用户再熟悉不过的“此电脑/这台电脑/我的电脑”页面） —— 错误源于外部因素，换句话说：**不是你的责任，但你不得不将用户阻断。** 此时，必须如示例一样：
	- 诚实披露**你所掌握的全部的（而非有限度的）** 真实错误原因与技术细节。若碍于篇幅，则提供可查询的错误码；
	- 有什么你可以做的：如果可能，提供立即从故障中恢复的尝试入口，或是将用户引导至舒适区；

宗旨：

- 当出现错误时，不要要求用户主动阅读任何说明文本，哪怕这段文本极其简单易懂；
	- 以开发者视角，错误是早已遇见的、理性处理的，
	- 但以用户视角，错误却是突然的意外，**并非所有用户都可以独立面对技术意外。**
	- **用户要的是解决错误，而不是理解错误。** 但是，也不可因此剥夺用户理解错误的权利。
- 作为替代，提供最为简单的一键恢复入口：
	- 不需要额外的学习或精力，
	- 只需用户点头，立即尝试将问题解决好，或是带用户来到他的舒适区。


# 技术规范

## 技术栈篇

- 使用 Web 技术实现前端 UI 时，避免第三方 UI 库，例如 `Ant Design`, HeroUI 等。在 AI 时代，UI 库的快速原型能力以及成型审美优势已经有所削弱，且大量 UI 库具有相当复杂的样式系统，不同版本之间亦存在兼容问题，任何 UI 定制化需求都可能面临未知的困难：精细化的 CSS 覆写需要耗费大量开发时间和 tokens，且仍可能引入隐性的、延迟爆发的样式冲突。不使用第三方样式系统，便是最好的样式方案。
- 使用 Web 技术实现前端 UI 时，避免 TailwindCSS。当巨量不具备上下文的 classname 充满 DOM 树，即使一些节点仍存在带有业务语义的特征，仍然会给人类调试时带来阻碍——几乎完全失去了从人类语言层面指代某节点的依据。在 AI 开发时代之前，此问题已经存在，如今则可能有所加剧。
- 避免使用 Qt, QML, Windows App SDK 等具有类 Web 特性，但实际上并非 Web 技术的框架。这些框架对 Web 特性存在不完整的、非标准的、非预期的支持，需要大量前期工作（文档阅读等）确保开发质量， 加之以 Qt 为例的标准库近年愈加臃肿，真原生的优势已经大大减弱，如果坚持使用它，在如今的技术体系中是得不偿失的——不论是内存占用、包体大小还是开发效率，Web 相对更优。

## 功能迭代篇

慎重做出删除功能入口的决定。这会给用户 “厂商似乎不喜欢这个功能 / 厂商似乎后悔提供这个功能 / 厂商似乎不希望用户总是用这个功能” 的危机感。

## 异步任务篇

- 长异步任务必须提供暂停与结束的能力。
### SplashScreen

- 原则上，应注重冷启动速度，不在启动时阻塞式加载资源。但是对于确有必要资源需要加载的情况，引入启动时 Loading 交互；
- 提供取消加载按钮，用于立即停止加载、退出程序；
- SplashScreen 窗口的事件循环需要保持干净，不可被阻塞形成假死。

### 思考资源释放的必要性

在进程结束后，其曾经持有的 fd、handle、内存页、内核对象、线程自然会被现代操作系统回收。在程序退出时，特别是当用户要求程序退出时，慎重考虑手动释放资源的必要性。检查：

- 释放是否昂贵
- 是否有double free风险
- 是否一定要等待线程结束

如无必要，将善后工作留给操作系统。

#### 例外：内存数据安全

仍有必要及时将内存中的敏感数据覆写，操作系统不一定会及时完成这一点。

#### 例外：写IO阻塞

当遗弃或是通过平台 API 强行销毁线程时，需确保不会对外存产生脏数据。写IO阻塞是不适用强行销毁线程的场景之一，用户要求程序退出时，若有IO阻塞，需给出 Blocking 弹窗：与 SplashScreen 性质类似，Blocking 的任务结束后自然关闭，也提供无视数据风险、立即退出的选项。

# 宏观规范

**发挥 AI 最大的想象力，试着同时理解：和谐、干练、洁净、实用、克制、灵动。**

# Proudly Crafted With CakeDesign

（可选）如果您的软件作品完整遵守了 CD 的各项强制性和建议性规范，那么欢迎您在软件的 “关于” 或类似栏目的末尾，以正文或更低级别的字号附上：

```
Proudly Crafted With CakeDesign.
```
